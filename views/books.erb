<section>
	<div class="container flex-div">
		<form action=""> 
			<h2><%= current_user.username %>'s Library</h2>
			<div class="library">
				<% if @books.any? %>
					<% @books.each do |book|%>
						<p>
							<div class="link">
								<p><a href="/books/<%= book.id %>"><%= book.title %></a></p>
							</div>
						</p>
						<p style="text-align: center;">
							by <%= book.author %>
						</p><br>
					<% end %>
				<% else %>
					<p>No books found</p> 
				<% end %>
			</div>
			<div class="link">
				<p><a href="/add-book">Add more</a></p>
			</div>
			<div class="link">
				<p><a href="/all-books">Big library</a></p>
			</div>
		</form>
	</div>
</section>